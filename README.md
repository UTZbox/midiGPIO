# midiGPIO
An interface between hardware inputs and outputs (voltage I/O or relay outputs) and MIDI. It supports USB and serial MIDI and provides four 3.3 V voltage inputs and four 3.3 V voltage outputs.
